%h3
  .text-center
    Activity
    .badge.bg-info
      = @activities.count


- @activities.each do |activity|
  %ul.list-group
    %li.list-group-item 
      = activity.id
      = link_to activity.trackable, activity.trackable
      = activity.owner
      = activity.key